description: 401 인증 실패
content:
  application/json:
    schema:
      type: object
      properties:
        statusCode:
          type: integer
          example: 401
        message:
          type: string
          example: 인증이 필요합니다.
