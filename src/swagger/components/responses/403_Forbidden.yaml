description: 403 권한 없음
content:
  application/json:
    schema:
      type: object
      properties:
        statusCode:
          type: integer
          example: 403
        message:
          type: string
          example: 접근 권한이 없습니다.
