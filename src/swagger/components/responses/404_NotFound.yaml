description: 404 리소스를 찾을 수 없음
content:
  application/json:
    schema:
      type: object
      properties:
        statusCode:
          type: integer
          example: 404
        message:
          type: string
          example: 요청한 리소스를 찾을 수 없습니다.
